@using Master.Web.Components
@using Master.Module
@inherits MasterRazorPage<TModel>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@addTagHelper *, Master.Web.Core
@model ModuleFormItemViewModel
@{
	var column = Model.ColumnInfo;

	var renderer = !string.IsNullOrEmpty(column.Renderer) ? column.Renderer : "lay-textarea";
}
<div is="@renderer" value="@(Model.Value?.ToString())" :column-Info="{ColumnKey:'@column.ColumnKey',ColumnName:'@column.ColumnName',VerifyRules:'@column.VerifyRules',Tips:'@column.Tips'}"></div>

@*<div class="layui-form-item layui-form-text">
	<label class="layui-form-label">@column.ColumnName</label>
	<div class="layui-input-block">
		<textarea placeholder="请输入@(column.ColumnName)" lay-verify="@column.VerifyRules" class="layui-textarea" name="@column.ColumnKey">@(Model.Value?.ToString())</textarea>
	</div>
</div>*@