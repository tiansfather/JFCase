@using Master.Web.Components
@using Master.Module
@inherits MasterRazorPage<TModel>
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@addTagHelper *, Master.Web.Core
@model ModuleFormItemViewModel
@{
	var column = Model.ColumnInfo;
	bool ischeck = false;
	bool.TryParse(Model.Value?.ToString(), out ischeck);
	//var ischeck = Model.Value != null ?  Convert.ToBoolean(Model.Value) : false;

	var renderer = !string.IsNullOrEmpty(column.Renderer) ? column.Renderer : "lay-switch";
}

<div is="@renderer" :value="@(ischeck?"true":"false")" :column-Info="{ColumnKey:'@column.ColumnKey',ColumnName:'@column.ColumnName',VerifyRules:'@column.VerifyRules',Tips:'@column.Tips'}"></div>

@*<div class="@(inline?"layui-inline":"layui-form-item")">
	<label class="layui-form-label">@column.ColumnName</label>
	<div class="@(inline?"layui-input-inline":"layui-input-block")">
		<input type="checkbox" name="@column.ColumnKey" @(ischeck ? "checked" : "") lay-verify="@column.VerifyRules" lay-skin="switch" lay-text="ON|OFF" lay-filter="@column.ColumnKey">
	</div>
</div>*@